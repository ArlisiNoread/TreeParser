#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>

char* concatString(char* a, char* b);

char* concatString(char* a, char* b){
	//return strncat(a, b, strlen(a) + strlen(b));
	return "";
}

